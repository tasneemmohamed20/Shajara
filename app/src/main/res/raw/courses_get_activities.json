{
  "endpoints": {
    "get_activities": {
      "description": "Get all activities in a course module",
      "method": "GET",
      "moodle_function": "core_course_get_contents",
      "url": "/webservice/rest/server.php",
      "note": "Same as get_course_contents; filter by section/module to extract activities.",
      "request": {
        "wsfunction": "core_course_get_contents",
        "wstoken": "{{token}}",
        "moodlewsrestformat": "json",
        "courseid": "101",
        "options[0][name]": "sectionid",
        "options[0][value]": "1003"
      },
      "response": "See get_course_contents response — each section's `modules` array contains activities."
    }
  }
}